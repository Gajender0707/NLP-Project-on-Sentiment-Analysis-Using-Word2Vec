1.Use the Kaggle dataset 
2. Take the all preprocessing steps before train the Word2Vec model
3. Train Word2Vec model using the corpus which was created by using the Text.
4. Word Embedding using the Word2Vec then Create the Vectors from the texts.
5. Train the model using the Naive Bayes Classifier.
6. Check all the accuracy on metrics.